services:
  postgres:
    image: postgres:16
    container_name: shopscrap-postgres
    environment:
      POSTGRES_DB: shopscrap
      POSTGRES_USER: shopscrap
      POSTGRES_PASSWORD: localdev
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data: